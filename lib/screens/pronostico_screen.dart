import 'package:flutter/material.dart';

class PronosticoScreen extends StatelessWidget {
  const PronosticoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}