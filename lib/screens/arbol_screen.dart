import 'package:flutter/material.dart';

class ArbolScreen extends StatelessWidget {
  const ArbolScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}